<div class="row align-center mb-5">
  <p><strong>Details</strong></p>
  <ion-button href="/publishers" shape="round">Back</ion-button>
  <ion-card>
    <ion-card-header>
      <ion-card-title>Games Count: {{publisher.games_count}}</ion-card-title>
      <ion-card-subtitle>{{publisher.name}}</ion-card-subtitle>
    </ion-card-header>
    <ion-card-content>
      {{{publisher.description}}}
    </ion-card-content>
  </ion-card>
  <ion-grid>
    <ion-row>
      {{#each publishers as |game|}}
        <ion-col size-xs="12" size-sm="12" size-md="6" size-lg="4">
          <a href="/games/{{game.id}}">
            <ion-card>
              <ion-card-header>
                <ion-card-title>Released: {{game.released}}</ion-card-title>
                <ion-card-subtitle>{{game.name}}</ion-card-subtitle>
              </ion-card-header>
              <ion-card-content>
                <img src={{game.background_image}} alt={{game.slug}} />
                {{game.esrb_rating.name}}
                {{game.rating}}
              </ion-card-content>
            </ion-card>
          </a>
        </ion-col>
      {{/each}}
    </ion-row>
  </ion-grid>
</div>
